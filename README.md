# Satellite Pass API
Given:

a satellite
an observer's latitude
longitude
altitude
the API calculates when that satellite will be visible above a minimum elevation.

However, this is not the main goal behind the project. Rather, the goal behind this project was to showcase an automated multi-enviroment deployment pipeline.
